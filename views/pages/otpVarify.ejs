<!DOCTYPE html>
<html lang="en">
<%- include("../components/head") -%>
<body>
    <div class="container d-flex justify-content-center align-items-center vh-100">
        <div class="card" style="width: 100%; max-width: 400px;">
            <div class="card-body">
                <h2 class="card-title text-center">Verify OTP</h2>
                <p class="text-center">Enter the OTP sent to your email address to verify your account.</p>
                
                <form action="/opt-verify" method="post">
                    <div class="mb-3">
                        <input type="text" name="id" value="<%= user._id %>" hidden>
                        <label for="otp" class="form-label">OTP :-</label>
                        <input type="text" id="otp" name="otp" class="form-control" placeholder="Enter OTP" maxlength="4">
                    </div>
                    
                    <button type="submit" class="btn btn-primary w-100">Verify OTP</button>
                </form>

                <div class="mt-3 text-center">
                    <p>If you didn't receive an OTP, <a href="/resend-otp">click here</a> to resend.</p>
                </div>
            </div>
        </div>
    </div>
</body>
</html>